# A Next.js, Redux, MySQL, Express Boilerplate

A next.js, redux, mysql ready boilerplate for my—and any other people's who want to use it—future projects. It overrides the default next.js server and its app and error page.

Just run
`npm install`
and then
`npm run start` to run it.
